# 图像分类类库

> 模块：图像分类
> 更新时间：2026-03-29

---

## 一、核心概念

### 图像分类原理

```
输入图像 → 卷积层(特征提取) → 池化层(降维) → 全连接层 → 分类输出

卷积层: 3x3/5x5卷积核滑动，提取局部特征
池化层: MaxPooling/AvgPooling，减少参数
全连接层: 将特征映射到类别概率
Softmax: 将输出转为概率分布
```

---

## 二、常用类库

### 1. timm (PyTorch Image Models)

**特点**：收录最多预训练模型的库，1000+模型

```bash
pip install timm
```

```python
import timm
import torch
from PIL import Image

# 加载模型
model = timm.create_model('resnet50', pretrained=True)
model.eval()

# 图片预处理
data_config = timm.data.resolve_model_data_config(model)
transforms = timm.data.create_transform(**data_config, is_training=False)

# 推理
img = Image.open('cat.jpg').convert('RGB')
tensor = transforms(img).unsqueeze(0)

with torch.no_grad():
    output = model(tensor)
    
# 获取预测类别
probabilities = torch.nn.functional.softmax(output[0], dim=0)
top5_prob, top5_catid = torch.topk(probabilities, 5)

# ImageNet标签
labels = timm.data.get_imagenet_labels()
for i in range(5):
    print(f"{labels[top5_catid[i]]}: {top5_prob[i].item():.2%}")
```

### 2. torchvision

**特点**：PyTorch官方视觉库，预训练模型丰富

```bash
pip install torchvision
```

```python
import torchvision.models as models
import torchvision.transforms as transforms
import torch
from PIL import Image

# 加载ResNet50
model = models.resnet50(weights=models.ResNet50_Weights.DEFAULT)
model.eval()

# 图片预处理
preprocess = transforms.Compose([
    transforms.Resize(256),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225]),
])

img = Image.open('cat.jpg')
img_t = preprocess(img)
batch_t = torch.unsqueeze(img_t, 0)

# 推理
with torch.no_grad():
    output = model(batch_t)

# 获取Top-5预测
_, indices = torch.sort(output, descending=True)
probabilities = torch.nn.functional.softmax(output[0], dim=0)

# CATEGORIES列表需要单独加载
print(f"预测类别ID: {indices[0].item()}, 概率: {probabilities[indices[0]].item():.2%}")
```

### 3. PyTorch Image Models (EfficientNet)

```python
import timm

# 使用EfficientNet-B3（精度高、速度快）
model = timm.create_model('efficientnet_b3', pretrained=True, num_classes=10)

# 迁移学习：替换分类头
model = timm.create_model('efficientnet_b3', pretrained=True, num_classes=10)

# 数据增强
from timm.data import create_transform
train_transform = create_transform(
    input_size=300,
    is_training=True,
    auto_augment='rand-m9-mstd0.5',
    interpolation='bicubic'
)

val_transform = create_transform(
    input_size=300,
    is_training=False,
    interpolation='bicubic'
)
```

---

## 三、完整训练代码

```python
import timm
import torch
import torch.nn as nn
from torch.utils.data import DataLoader
from torchvision import transforms
from PIL import Image
import os

class ImageDataset(torch.utils.data.Dataset):
    def __init__(self, root_dir, transform=None):
        self.root_dir = root_dir
        self.transform = transform
        self.classes = sorted(os.listdir(root_dir))
        self.class_to_idx = {c: i for i, c in enumerate(self.classes)}
        self.images = []
        
        for cls in self.classes:
            cls_dir = os.path.join(root_dir, cls)
            for img_name in os.listdir(cls_dir):
                self.images.append((os.path.join(cls_dir, img_name), self.class_to_idx[cls]))
    
    def __len__(self):
        return len(self.images)
    
    def __getitem__(self, idx):
        img_path, label = self.images[idx]
        img = Image.open(img_path).convert('RGB')
        if self.transform:
            img = self.transform(img)
        return img, label

# 数据预处理
train_transform = transforms.Compose([
    transforms.Resize(256),
    transforms.RandomResizedCrop(224),
    transforms.RandomHorizontalFlip(),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])
])

# 创建数据加载器
train_dataset = ImageDataset('data/train', transform=train_transform)
train_loader = DataLoader(train_dataset, batch_size=32, shuffle=True)

# 加载预训练模型并微调
model = timm.create_model('efficientnet_b0', pretrained=True, num_classes=5)
model = model.cuda()

criterion = nn.CrossEntropyLoss()
optimizer = torch.optim.AdamW(model.parameters(), lr=1e-4)

# 训练
for epoch in range(10):
    model.train()
    for images, labels in train_loader:
        images, labels = images.cuda(), labels.cuda()
        
        optimizer.zero_grad()
        outputs = model(images)
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()
    
    print(f"Epoch {epoch+1}, Loss: {loss.item():.4f}")

# 保存模型
torch.save(model.state_dict(), 'model.pth')
```

---

## 四、模型对比

| 模型 | Top-1准确率 | 参数量 | 推理速度 |
|------|------------|--------|----------|
| ResNet50 | 76.1% | 25.6M | 中等 |
| EfficientNet-B0 | 77.1% | 5.3M | 快 |
| EfficientNet-B3 | 81.6% | 12.2M | 中等 |
| Vision Transformer | 80.1% | 86.4M | 慢 |

---

*下一步：目标检测类库*
