# 双亲委派模型

> 模块：类加载器
> 更新时间：2026-03-29

---

## 一、类加载器概述

类加载器（ClassLoader）负责将Class文件加载到JVM中，生成Class对象。

**三层类加载器架构**：
```
Bootstrap ClassLoader → Extension ClassLoader → Application ClassLoader → Custom ClassLoader
   (C++实现)            (Java实现)           (Java实现)            (用户自定义)
```

---

## 二、双亲委派模型

### 核心原理

```java
public abstract class ClassLoader {
    
    private final ClassLoader parent;
    
    protected Class<?> loadClass(String name, boolean resolve) 
            throws ClassNotFoundException {
        
        // 1. 检查是否已加载
        Class<?> clazz = findLoadedClass(name);
        if (clazz != null) {
            return clazz;
        }
        
        try {
            // 2. 委托给父加载器
            if (parent != null) {
                clazz = parent.loadClass(name, false);
            } else {
                // 3. 尝试Bootstrap加载器
                clazz = findBootstrapClassOrNull(name);
            }
        } catch (ClassNotFoundException e) {
            // 父加载器找不到
        }
        
        // 4. 自己加载
        if (clazz == null) {
            clazz = findClass(name);
        }
        
        return clazz;
    }
}
```

---

## 三、自定义类加载器

```java
public class FileClassLoader extends ClassLoader {
    
    private String basePath;
    
    public FileClassLoader(String basePath) {
        this.basePath = basePath;
    }
    
    @Override
    protected Class<?> findClass(String name) throws ClassNotFoundException {
        String fileName = name.replace('.', '/') + ".class";
        String fullPath = basePath + "/" + fileName;
        
        try (FileInputStream fis = new FileInputStream(fullPath);
             ByteArrayOutputStream bos = new ByteArrayOutputStream()) {
            
            byte[] data = bos.toByteArray();
            return defineClass(name, data, 0, data.length);
            
        } catch (IOException e) {
            throw new ClassNotFoundException(name, e);
        }
    }
}

// 使用
FileClassLoader loader = new FileClassLoader("/path/to/classes");
Class<?> clazz = loader.loadClass("com.example.MyClass");
```

---

## 四、线程上下文类加载器

```java
// JDK服务发现机制
public class ServiceLoader<S> {
    public Iterator<S> iterator() {
        ClassLoader cl = Thread.currentThread().getContextClassLoader();
        // 使用线程上下文类加载器加载服务提供者
    }
}

// SPI机制：JDBC驱动加载
// DriverManager使用线程上下文类加载器加载MySQL/Oracle驱动
```

---

## 五、总结

双亲委派模型是JVM类加载的核心机制，保证了类的唯一性和安全性。

**学习要点**：
1. 理解三层类加载器架构
2. 掌握双亲委派的执行流程
3. 理解为什么要用双亲委派
4. 掌握自定义类加载器的实现

---

*下一步：运行时数据区*
