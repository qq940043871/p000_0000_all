# 任务规划

> 模块：Agent编排
> 更新时间：2026-03-29

---

## 一、任务分解

```python
class TaskPlanner:
    """任务规划器"""
    
    def __init__(self, llm: LLM):
        self.llm = llm
    
    async def decompose(self, task: str) -> List[Task]:
        """分解任务"""
        prompt = f"""将以下任务分解为可执行的子任务：

任务：{task}

请列出具体的执行步骤，每步说明做什么。"""
        
        response = await self.llm.generate(prompt)
        
        # 解析为任务列表
        tasks = self._parse_tasks(response)
        return tasks
    
    async def refine(self, tasks: List[Task]) -> List[Task]:
        """优化任务顺序"""
        # 检查依赖关系
        # 去除冗余
        # 优化并行度
        return tasks
```

---

*下一步：工具选择*
