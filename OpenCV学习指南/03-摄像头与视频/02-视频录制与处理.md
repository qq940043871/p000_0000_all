# 视频录制与处理

> 模块：摄像头与视频
> 更新时间：2026-04-06

---

## 一、视频录制基础

### 1.1 基本录制

```python
import cv2

cap = cv2.VideoCapture(0)

width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
fps = 30.0

fourcc = cv2.VideoWriter_fourcc(*'XVID')
out = cv2.VideoWriter('output.avi', fourcc, fps, (width, height))

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    out.write(frame)
    cv2.imshow('Recording', frame)
    
    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

cap.release()
out.release()
cv2.destroyAllWindows()
```

### 1.2 编码器选择

```python
fourcc = cv2.VideoWriter_fourcc(*'XVID')

fourcc = cv2.VideoWriter_fourcc(*'mp4v')

fourcc = cv2.VideoWriter_fourcc(*'H264')

fourcc = cv2.VideoWriter_fourcc(*'MJPG')

fourcc = cv2.VideoWriter_fourcc(*'FFV1')
```

### 1.3 带音频录制

```python
import cv2
import subprocess
import os

cap = cv2.VideoCapture(0)
width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))

fourcc = cv2.VideoWriter_fourcc(*'mp4v')
video_path = 'temp_video.mp4'
out = cv2.VideoWriter(video_path, fourcc, 30.0, (width, height))

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    out.write(frame)
    cv2.imshow('Recording', frame)
    
    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

cap.release()
out.release()
cv2.destroyAllWindows()
```

---

## 二、视频文件处理

### 2.1 读取视频文件

```python
import cv2

cap = cv2.VideoCapture('input.mp4')

fps = cap.get(cv2.CAP_PROP_FPS)
width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
total_frames = int(cap.get(cv2.CAP_PROP_FRAME_COUNT))
duration = total_frames / fps

print(f"FPS: {fps}, 分辨率: {width}x{height}, 时长: {duration:.2f}秒")

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    cv2.imshow('Video', frame)
    
    if cv2.waitKey(int(1000/fps)) & 0xFF == ord('q'):
        break

cap.release()
cv2.destroyAllWindows()
```

### 2.2 视频转码

```python
import cv2

cap = cv2.VideoCapture('input.avi')

width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
fps = cap.get(cv2.CAP_PROP_FPS)

fourcc = cv2.VideoWriter_fourcc(*'mp4v')
out = cv2.VideoWriter('output.mp4', fourcc, fps, (width, height))

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    out.write(frame)

cap.release()
out.release()
```

### 2.3 视频裁剪

```python
import cv2

cap = cv2.VideoCapture('input.mp4')

fps = cap.get(cv2.CAP_PROP_FPS)
width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))

fourcc = cv2.VideoWriter_fourcc(*'mp4v')
out = cv2.VideoWriter('clipped.mp4', fourcc, fps, (width, height))

start_frame = int(10 * fps)
end_frame = int(30 * fps)

cap.set(cv2.CAP_PROP_POS_FRAMES, start_frame)

frame_count = 0
while cap.isOpened():
    ret, frame = cap.read()
    if not ret or frame_count >= (end_frame - start_frame):
        break
    
    out.write(frame)
    frame_count += 1

cap.release()
out.release()
```

---

## 三、视频特效处理

### 3.1 调整分辨率

```python
import cv2

cap = cv2.VideoCapture('input.mp4')
fps = cap.get(cv2.CAP_PROP_FPS)

new_width, new_height = 1280, 720
fourcc = cv2.VideoWriter_fourcc(*'mp4v')
out = cv2.VideoWriter('resized.mp4', fourcc, fps, (new_width, new_height))

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    resized = cv2.resize(frame, (new_width, new_height))
    out.write(resized)

cap.release()
out.release()
```

### 3.2 添加水印

```python
import cv2

cap = cv2.VideoCapture('input.mp4')
fps = cap.get(cv2.CAP_PROP_FPS)
width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))

logo = cv2.imread('logo.png', cv2.IMREAD_UNCHANGED)
logo = cv2.resize(logo, (100, 50))

fourcc = cv2.VideoWriter_fourcc(*'mp4v')
out = cv2.VideoWriter('watermarked.mp4', fourcc, fps, (width, height))

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    h, w = logo.shape[:2]
    x, y = width - w - 10, height - h - 10
    
    if logo.shape[2] == 4:
        alpha = logo[:, :, 3] / 255.0
        for c in range(3):
            frame[y:y+h, x:x+w, c] = (1 - alpha) * frame[y:y+h, x:x+w, c] + alpha * logo[:, :, c]
    else:
        frame[y:y+h, x:x+w] = logo
    
    out.write(frame)

cap.release()
out.release()
```

### 3.3 添加时间戳

```python
import cv2
from datetime import datetime, timedelta

cap = cv2.VideoCapture('input.mp4')
fps = cap.get(cv2.CAP_PROP_FPS)
width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))

fourcc = cv2.VideoWriter_fourcc(*'mp4v')
out = cv2.VideoWriter('timestamped.mp4', fourcc, fps, (width, height))

start_time = datetime(2026, 1, 1, 0, 0, 0)
frame_count = 0

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    current_time = start_time + timedelta(seconds=frame_count / fps)
    timestamp = current_time.strftime("%Y-%m-%d %H:%M:%S")
    
    cv2.putText(frame, timestamp, (10, height - 20),
                cv2.FONT_HERSHEY_SIMPLEX, 1, (255, 255, 255), 2)
    
    out.write(frame)
    frame_count += 1

cap.release()
out.release()
```

---

## 四、视频分析

### 4.1 帧差法运动检测

```python
import cv2

cap = cv2.VideoCapture('input.mp4')

ret, prev_frame = cap.read()
prev_gray = cv2.cvtColor(prev_frame, cv2.COLOR_BGR2GRAY)
prev_gray = cv2.GaussianBlur(prev_gray, (21, 21), 0)

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
    gray = cv2.GaussianBlur(gray, (21, 21), 0)
    
    diff = cv2.absdiff(prev_gray, gray)
    thresh = cv2.threshold(diff, 25, 255, cv2.THRESH_BINARY)[1]
    
    contours, _ = cv2.findContours(thresh, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    
    for contour in contours:
        if cv2.contourArea(contour) < 500:
            continue
        x, y, w, h = cv2.boundingRect(contour)
        cv2.rectangle(frame, (x, y), (x + w, y + h), (0, 255, 0), 2)
    
    cv2.imshow('Motion Detection', frame)
    
    prev_gray = gray
    
    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

cap.release()
cv2.destroyAllWindows()
```

### 4.2 背景减除

```python
import cv2

cap = cv2.VideoCapture('input.mp4')

bg_subtractor = cv2.createBackgroundSubtractorMOG2(history=500, varThreshold=16, detectShadows=True)

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    fg_mask = bg_subtractor.apply(frame)
    
    _, thresh = cv2.threshold(fg_mask, 200, 255, cv2.THRESH_BINARY)
    
    contours, _ = cv2.findContours(thresh, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    
    for contour in contours:
        if cv2.contourArea(contour) < 1000:
            continue
        x, y, w, h = cv2.boundingRect(contour)
        cv2.rectangle(frame, (x, y), (x + w, y + h), (0, 255, 0), 2)
    
    cv2.imshow('Background Subtraction', frame)
    cv2.imshow('Foreground Mask', fg_mask)
    
    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

cap.release()
cv2.destroyAllWindows()
```

---

## 五、视频截图

### 5.1 按帧截图

```python
import cv2
import os

cap = cv2.VideoCapture('input.mp4')
os.makedirs('frames', exist_ok=True)

frame_count = 0
save_interval = 30

while cap.isOpened():
    ret, frame = cap.read()
    if not ret:
        break
    
    if frame_count % save_interval == 0:
        cv2.imwrite(f'frames/frame_{frame_count:06d}.jpg', frame)
    
    frame_count += 1

cap.release()
print(f"保存了 {frame_count // save_interval} 张截图")
```

### 5.2 按时间截图

```python
import cv2

cap = cv2.VideoCapture('input.mp4')
fps = cap.get(cv2.CAP_PROP_FPS)

timestamps = [5, 10, 15, 20]

for i, ts in enumerate(timestamps):
    frame_number = int(ts * fps)
    cap.set(cv2.CAP_PROP_POS_FRAMES, frame_number)
    
    ret, frame = cap.read()
    if ret:
        cv2.imwrite(f'screenshot_{ts}s.jpg', frame)

cap.release()
```

---

## 六、实用工具函数

### 6.1 获取视频信息

```python
def get_video_info(video_path):
    cap = cv2.VideoCapture(video_path)
    
    info = {
        'width': int(cap.get(cv2.CAP_PROP_FRAME_WIDTH)),
        'height': int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT)),
        'fps': cap.get(cv2.CAP_PROP_FPS),
        'frame_count': int(cap.get(cv2.CAP_PROP_FRAME_COUNT)),
        'duration': cap.get(cv2.CAP_PROP_FRAME_COUNT) / cap.get(cv2.CAP_PROP_FPS),
        'fourcc': int(cap.get(cv2.CAP_PROP_FOURCC))
    }
    
    cap.release()
    return info

info = get_video_info('input.mp4')
print(f"分辨率: {info['width']}x{info['height']}")
print(f"帧率: {info['fps']} FPS")
print(f"时长: {info['duration']:.2f} 秒")
```

### 6.2 视频拼接

```python
def concat_videos(video_paths, output_path):
    cap = cv2.VideoCapture(video_paths[0])
    width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
    height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
    fps = cap.get(cv2.CAP_PROP_FPS)
    cap.release()
    
    fourcc = cv2.VideoWriter_fourcc(*'mp4v')
    out = cv2.VideoWriter(output_path, fourcc, fps, (width, height))
    
    for path in video_paths:
        cap = cv2.VideoCapture(path)
        while cap.isOpened():
            ret, frame = cap.read()
            if not ret:
                break
            out.write(frame)
        cap.release()
    
    out.release()

concat_videos(['part1.mp4', 'part2.mp4', 'part3.mp4'], 'merged.mp4')
```

---

*文档完成*
